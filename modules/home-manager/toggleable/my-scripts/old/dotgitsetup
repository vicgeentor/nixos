#!/usr/bin/env bash

alias dotgit='/usr/bin/git --git-dit=$HOME/Git/vicgeentor/dotfiles-xx/ --work-tree=$HOME'

git clone git@github.com:vicgeentor/dotfiles-xx.git "$HOME"/Git/vicgeentor/dotfiles-xx

rm -rf .profile .bashrc

dotgit checkout
dotgit config --local status.showUntrackedFiles no
